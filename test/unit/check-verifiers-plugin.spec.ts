

describe('Check verifier plugin test', () => {
    beforeEach(() => {});
    it('Creates correct urls for android device and application installed', (done) => {
        console.log('does this work at all?');
        return true;
    });
});
